import { AutenticationModule } from './autentication.module';

describe('Autentication.Module', () => {
  it('should create an instance', () => {
    expect(new AutenticationModule()).toBeTruthy();
  });
});
