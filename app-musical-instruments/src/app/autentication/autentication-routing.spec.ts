import { AutenticationRouting } from './autentication-routing';

describe('AutenticationRouting', () => {
  it('should create an instance', () => {
    expect(new AutenticationRouting()).toBeTruthy();
  });
});
